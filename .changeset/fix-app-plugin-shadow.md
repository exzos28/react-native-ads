---
"@react-native-ads/pangle": patch
"@react-native-ads/levelplay": patch
---

Rename the shadowed `config` parameter in the Gradle properties mod callback to `modConfig`, fixing an eslint `no-shadow` warning in `app.plugin.js`. No behavior change.
